# LiteLLM Semantic Routing Gateway Demo - Environment Configuration
#
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#
# IMPORTANT: Never commit .env to version control!

#############################################################################
# Ziti Router Enrollment Tokens
#############################################################################
# Generated by setup-ziti.bash - copy the JWT contents here
# These are one-time-use tokens; once enrolled, routers persist their identity

LITELLM_ZITI_ENROLL_TOKEN=
OLLAMA_ZITI_ENROLL_TOKEN=

#############################################################################
# Public LLM Provider API Keys (for fallback routing)
#############################################################################
# At least one of these is required for the public model fallback
# Get keys from: https://openrouter.ai, https://platform.openai.com, etc.

OPENROUTER_API_KEY=
OPENAI_API_KEY=
ANTHROPIC_API_KEY=

#############################################################################
# Optional: LiteLLM Authentication
#############################################################################
# Required when LiteLLM is publicly accessible (e.g., via zrok)
# When set, all API requests must include: Authorization: Bearer <LITELLM_MASTER_KEY>

# LITELLM_MASTER_KEY=sk-your-secret-key   # Must start with 'sk-'. Required for API and Admin UI access.
# LITELLM_SALT_KEY=sk-your-salt-key       # IMPORTANT: Cannot change once set! Used to encrypt credentials in DB.
# UI_USERNAME=admin
# UI_PASSWORD=admin

#############################################################################
# Optional: Public Sharing via zrok.io (compose.zrok.yaml)
#############################################################################
# Get your token at https://myzrok.io

# ZROK_ENABLE_TOKEN=
# ZROK_UNIQUE_NAME=                                      # REQUIRED: unique subdomain (4-32 chars, lowercase alphanumeric)
# ZROK_ENVIRONMENT_NAME=litellm gateway public share  # how this environment will appear in the zrok console

# Optional: zrok OAuth authentication
# ZROK_OAUTH_PROVIDER=google                             # google or github
# ZROK_OAUTH_EMAILS=*@mycompany.com admin@example.com   # space-separated email patterns

# ZROK_API_ENDPOINT=https://api.zrok.io  # set if self-hosting and not using zrok.io from NetFoundry

#############################################################################
# Optional: Ziti Router Image Override
#############################################################################

# Use a specific ziti-router image version
# ZITI_ROUTER_IMAGE=openziti/ziti-router:1.2.3
